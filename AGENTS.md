# AGENTS.md

قوانین کاریِ این ریپو — هر تغییر اصلی باید این‌ها را رعایت کند.

## قانون کلیدی
- بعد از **هر** کار/تغییر معنادار (فایل، محتوا، ساختار، ویژگی، انتشار)، فایل `README.md` را هم به‌روز کن و در همان commit قرار بده (مگر آنکه تغییری صرفاً داخلی و فنیِ بی‌اثر روی مستندات باشد).

## کنوانسیون‌ها
- زبان فایل‌ها و محتوا: فارسی.
- تغییر در `styles.css` یا `app.js` → شماره‌ی نسخه در `index.html` را بالا ببر (مثل `app.js?v=N`) تا کش مرورگر به‌روز شود.
- ساختار داده (تغییرناپذیر مگر با دلیل):
  - `data/books/manifest.json` → لیست کتاب‌ها
  - `data/books/<book>/book.json` → مشخصات کتاب
  - `data/books/<book>/chapters.json` → فهرست فصل‌ها + تعداد سکشن (فیلد `sections`)
  - `data/books/<book>/chapterIndex.json` → متادیتای ساختار (بخش‌ها/سکشن‌ها) برای لود سریع صفحه‌ی کتاب — بعد از تغییر فایل‌های فصل باید **بازتولید/یکدست** بماند
  - `data/books/<book>/chapters/<chN>.json` → تعریف فصل؛ `<chN>/part<K>.json` → محتوای بخش
- هر فصل ۴-۶ بخش (`p1..pK`) و هر بخش ۵-۸ سکشن (`s1..sN` متوالی). کلید سکشن = `pX_sY`.
- پیشرفت/یادداشت/آخرین مطالعه در localStorage است؛ ساختار ذخیره را بدون احتیاط نشکن.

## تست
- JSON را با `python -X utf8 -c "import json; json.load(open(f,encoding='utf-8'))"` اعتبارسنجی کن.
- رندر را با سرور محلی چک کن: `python -m http.server 8000` سپس `http://localhost:8000`.
- حتماً صفحه‌ی خانگی، صفحه‌ی کتاب و مسیر خواننده (`#/ch/<bookId>/<chId>/read/<pX_sY>`) را تست کن.